# Copy to .env.local (Vite convention; gitignored) and adjust as needed.

# Base URL the axios client sends requests to. In dev this is normally left
# as-is and requests go through the Vite proxy configured in vite.config.ts;
# set it explicitly when pointing at a non-local backend.
VITE_API_BASE_URL=/api/v1
